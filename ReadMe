//// Development. /////////

#DB dev setup#
1. Open PG admin and create an empty db in local called -> memory_development
2. Install node_modules for first time -> npm install 
2. Run db migration command -> npm run dbmigrate:dev


#To run in local use the below command
npm run start:dev
